/* global React, AuraUI */
export function KPIStrip() {
  const { Container, Eyebrow } = AuraUI;
  const kpis = [
    ["MW INSTALADOS", "312", "en operación"],
    ["EN CONSTRUCCIÓN", "480 MW", "horizonte 2026"],
    ["HECTÁREAS", "4 200", "bajo gestión"],
    ["CO₂ EVITADO", "185 Kt", "anuales"],
  ];
  return (
    <section style={{ background: "var(--paper)", padding: "80px 0", borderBottom: "1px solid var(--border-subtle)" }}>
      <Container>
        <Eyebrow style={{ marginBottom: 12 }}>NÚMEROS · CIERRE 2024</Eyebrow>
        <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-end", marginBottom: 56, gap: 32, flexWrap: "wrap" }}>
          <h2 style={{ fontFamily: "var(--font-display)", fontWeight: 700, fontSize: "clamp(36px, 4.4vw, 60px)", letterSpacing: "-0.02em", lineHeight: 1.05, margin: 0, maxWidth: 760, textWrap: "balance" }}>
            Cuatro parques, una sola red, ocho años operando.
          </h2>
          <p style={{ fontFamily: "var(--font-body)", fontSize: 16, color: "var(--fg-2)", maxWidth: 320, lineHeight: 1.6, margin: 0 }}>
            Optimizamos el factor de capacidad combinando recursos solar y eólico en una única plataforma de despacho.
          </p>
        </div>
        <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 32 }}>
          {kpis.map(([k,v,sub]) => (
            <div key={k} style={{ borderLeft: "2px solid var(--aura-petrol)", paddingLeft: 20 }}>
              <div style={{ fontFamily: "var(--font-eyebrow)", textTransform: "uppercase", letterSpacing: "0.18em", fontSize: 11, color: "var(--fg-3)", marginBottom: 14 }}>{k}</div>
              <div style={{ fontFamily: "var(--font-display)", fontWeight: 700, fontSize: 64, lineHeight: 0.95, letterSpacing: "-0.025em", color: "var(--aura-petrol)" }}>{v}</div>
              <div style={{ fontFamily: "var(--font-body)", fontSize: 13, color: "var(--fg-3)", marginTop: 10 }}>{sub}</div>
            </div>
          ))}
        </div>
      </Container>
    </section>
  );
}

window.KPIStrip = KPIStrip;
